import { TransformLenghtPipe } from './transform-lenght-pipe';

describe('TransformLenghtPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformLenghtPipe();
    expect(pipe).toBeTruthy();
  });
});
